# Features
Feature-sliced modules. Each subfolder owns its own UI components, hooks, API client wrappers, and Redux/Query keys.

Planned:
- `auth/` (Phase 2)
- `products/` (Phase 3-4)
- `inquiries/` (Phase 6)
- `dashboard/` (Phase 7)
- `admin/` (Phase 8)
