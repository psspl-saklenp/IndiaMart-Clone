# Migrations
Sequelize-CLI migrations live here. Created in Phase 2 onwards.

Generate a new migration:

```
npx sequelize-cli migration:generate --name create-users --config src/database/sequelize-cli.config.js --migrations-path src/database/migrations
```
