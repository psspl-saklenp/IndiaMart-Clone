# Models
Sequelize-TypeScript models. Each feature module (users, products, …) declares its own model under `src/modules/<feature>/<feature>.model.ts` and re-exports it here for cross-module references.
